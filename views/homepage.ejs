<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>PokéCollector</title>
  <link rel="icon" href="/images/pokeball.png">
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      margin: 0;
      background: linear-gradient(to bottom right, #fff9c4, #ffccbc, #bbdefb);
      text-align: center;
      color: #333;
    }

    nav {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 1rem 2rem;
      background: rgba(255, 255, 255, 0.6);
      backdrop-filter: blur(8px);
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
      position: sticky;
      top: 0;
    }

    nav .logo {
      display: flex;
      align-items: center;
      gap: 10px;
      font-weight: 700;
      font-size: 1.2rem;
      color: #d32f2f;
    }

    nav .logo img {
      width: 28px;
      height: 28px;
    }

    nav ul {
      display: flex;
      gap: 1.5rem;
      list-style: none;
      margin: 0;
      padding: 0;
    }

    nav a {
      text-decoration: none;
      color: #333;
      font-weight: 500;
      transition: color 0.2s ease;
    }

    nav a:hover {
      color: #e53935;
    }

    .hero {
      margin-top: 4rem;
      padding: 2rem;
    }

    .hero h1 {
      font-size: 3rem;
      font-weight: 800;
      color: #d32f2f;
      text-shadow: 1px 1px 2px rgba(0,0,0,0.1);
    }

    .hero p {
      max-width: 600px;
      margin: 1rem auto;
      font-size: 1.1rem;
      color: #444;
    }

    .buttons {
      margin-top: 1.5rem;
    }

    .btn {
      display: inline-block;
      padding: 0.8rem 1.6rem;
      margin: 0.5rem;
      border-radius: 30px;
      font-weight: 600;
      cursor: pointer;
      text-decoration: none;
      transition: all 0.3s ease;
    }

    .btn-primary {
      background-color: #e53935;
      color: white;
    }

    .btn-primary:hover {
      background-color: #c62828;
    }

    .btn-outline {
      border: 2px solid #e53935;
      color: #e53935;
    }

    .btn-outline:hover {
      background-color: #e53935;
      color: white;
    }

    .featured {
      margin: 4rem auto;
      display: grid;
      gap: 2rem;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      max-width: 900px;
      padding: 0 1rem;
    }

    .pokemon-card {
      background: rgba(255, 255, 255, 0.7);
      backdrop-filter: blur(10px);
      border-radius: 20px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
      padding: 1.5rem;
      transition: transform 0.2s ease;
    }

    .pokemon-card:hover {
      transform: translateY(-5px);
    }

    .pokemon-card img {
      width: 120px;
      height: 120px;
      object-fit: contain;
    }

    .pokemon-card h3 {
      margin-top: 0.5rem;
      font-size: 1.2rem;
      color: #222;
    }

    .pokemon-card p {
      color: #666;
      font-size: 0.9rem;
    }

    footer {
      margin-top: 4rem;
      padding: 2rem;
      background: rgba(255,255,255,0.6);
      font-size: 0.9rem;
      color: #555;
    }
  </style>
</head>
<body>
  <%- include('navbar.ejs') %>

  <section class="hero">
    <h1>Catch 'Em All!</h1>
    <p>Build your ultimate Pokémon collection, track your catches, and explore the world of Pokémon right from your Pokédex.</p>
    <div class="buttons">
      <a href="#" class="btn btn-primary">View Collection</a>
      <a href="#" class="btn btn-outline">Catch Pokémon</a>
    </div>
  </section>

  <% if (sets && sets.length > 0) { %>
  <section class="featured">
    <% sets.forEach(set => { %>
    <a href="/setViewCards/<%= set.id %>" style="text-decoration: none; color: inherit;">
      <div class="pokemon-card">
        <img src="<%= set.images.logo %>" alt="<%= set.name %> Logo">
        <h3><%= set.name %></h3>
        <p>Release Date: <%= set.releaseDate %></p>
        <p>Series: <%= set.series %></p>
      </div>
    </a>
    <% }); %>
  </section>
  <% } %>
  <footer>
    © 2025 PokéCollector — Made with ❤️ by Trainers.
  </footer>
</body>
</html>
