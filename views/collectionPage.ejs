<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>PokéCollector | My Collection</title>
  <link rel="icon" href="/images/pokeball.png">
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      margin: 0;
      background: linear-gradient(to bottom right, #fff9c4, #ffccbc, #bbdefb);
      min-height: 100vh;
      color: #333;
      display: flex;
      flex-direction: column;
    }

    nav {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 1rem 2rem;
      background: rgba(255, 255, 255, 0.7);
      backdrop-filter: blur(10px);
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      position: sticky;
      top: 0;
      z-index: 10;
    }

    nav .logo {
      display: flex;
      align-items: center;
      gap: 10px;
      font-weight: 700;
      font-size: 1.2rem;
      color: #d32f2f;
    }

    nav img {
      width: 28px;
      height: 28px;
    }

    nav ul {
      list-style: none;
      display: flex;
      gap: 1.5rem;
      margin: 0;
      padding: 0;
    }

    nav a {
      text-decoration: none;
      color: #333;
      font-weight: 500;
      transition: color 0.2s;
    }

    nav a:hover {
      color: #e53935;
    }

    .btn {
      display: inline-block;
      padding: 0.8rem 1.6rem;
      margin: 0.5rem;
      border-radius: 30px;
      font-weight: 600;
      cursor: pointer;
      text-decoration: none;
      transition: all 0.3s ease;
    }

    .btn-primary {
      background-color: #e53935;
      color: white;
    }

    .btn-primary:hover {
      background-color: #c62828;
    }

    .btn-outline {
      border: 2px solid #e53935;
      color: #e53935;
    }

    .btn-outline:hover {
      background-color: #e53935;
      color: white;
    }

    main {
      flex: 1;
      padding: 2rem;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .binder-section {
      background-color: rgba(255,255,255,0.9);
      padding: 1.5rem 2rem;
      border-radius: 20px;
      box-shadow: 0 5px 15px rgba(0,0,0,0.1);
      width: 100%;
      max-width: 1100px;
    }

    .binder-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 2rem;
      flex-wrap: wrap;
      gap: 1rem;
    }

    .binder-header input {
      border: 2px solid #e53935;
      border-radius: 10px;
      padding: 0.6rem 1rem;
      font-size: 1.1rem;
      font-weight: 600;
      color: #e53935;
      background: #fff;
      width: 250px;
      transition: all 0.2s ease;
    }

    .binder-header input:focus {
      outline: none;
      border-color: #c62828;
      background: #fff3f3;
    }

    .binder-header h2 {
      color: #333;
      margin: 0;
      font-size: 1.3rem;
    }

    .card-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
      gap: 1.5rem;
    }
    .card {
      background: rgba(255, 255, 255, 0.8);
      border-radius: 16px;
      box-shadow: 0 6px 15px rgba(0,0,0,0.1);
      padding: 1rem;
      text-align: center;
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

    .card:hover {
      transform: translateY(-5px);
      box-shadow: 0 8px 20px rgba(0,0,0,0.15);
    }

    .card img {
      width: 100px;
      height: 100px;
      object-fit: contain;
    }

    .card h3 {
      margin: 0.5rem 0 0.2rem;
      font-size: 1.2rem;
      color: #222;
    }

    .card p {
      margin: 0;
      color: #666;
      font-size: 0.9rem;
    }

    footer {
      text-align: center;
      padding: 2rem;
      color: #666;
      font-size: 0.85rem;
      margin-top: 2rem;
      background: rgba(255,255,255,0.6);
    }
  </style>
</head>
<body>
  <%- include('navbar.ejs') %>

  <main>
    <section class="binder-section">
      <% if (cards && cards.length > 0) { %>
      <div class="binder-header">
        <h2><%=collection.userName%>'s Binder:</h2>
        <form action="/updateBinderName" method="POST" style="display: inline;">
          <input type="text" name="binderName" class="binder-name" value="<%=collection.binderName%>" />
          <button type="submit" class="btn btn-primary">Update Binder Name</button>
        </form>
      </div>

      <div class="card-grid">
        <% cards.forEach(card => { %>
          <div class="card">
            <img src="<%=card.image%>/low.webp" alt="<%=card.name%>">
            <h3><%= card.name %> </h3>
            <a href="/deleteFromCollection/<%=card.id%>" class="btn btn-primary">Remove from Collection</a>
          </div>
        <% }); %>
      </div>
      <% } else { %>
        <p>No cards found in your collection.</p>
      <% } %>
      </section>
    </main>

  <footer>
    © 2025 PokéCollector — Keep catching 'em all!
  </footer>
</body>
</html>
